<UserControl x:Class="Aksl.Modules.Account.Views.ResetLockoutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ei="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:behaviors="clr-namespace:Aksl.Xaml.Behaviors;assembly=Aksl.Toolkit"
             xmlns:materialDesign="clr-namespace:Aksl.Toolkit.Controls;assembly=Aksl.Toolkit.Controls"
             xmlns:aksl="clr-namespace:Aksl.Toolkit.UI.Controls;assembly=Aksl.Toolkit"
             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>

        <ControlTemplate x:Key="UserNameRrrorTemplate">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <!-- 被装饰的控件 -->
                <AdornedElementPlaceholder Grid.Row="0"
                                           x:Name="Placeholder" />
                <!-- 错误提示放在下方 -->
                <StackPanel Grid.Row="1"
                            Orientation="Horizontal"
                            Margin="2,2,0,1">
                    <TextBlock Text="*"
                               Foreground="Red"
                               FontSize="10"
                               VerticalAlignment="Center" />
                    <TextBlock Text="{Binding ElementName=Placeholder,Path=AdornedElement.(Validation.Errors)[0].ErrorContent}"
                               Foreground="Red"
                               FontSize="15"
                               TextWrapping="Wrap"
                               Margin="4,0,0,0"
                               VerticalAlignment="Center" />
                </StackPanel>
            </Grid>
        </ControlTemplate>

        <Style x:Key="UserNameTextBoxStyle"
               TargetType="{x:Type TextBox}">
            <Setter Property="Height"
                    Value="40" />
            <Setter Property="FontSize"
                    Value="16" />
            <Setter Property="TextWrapping"
                    Value="Wrap" />
            <Setter Property="VerticalContentAlignment"
                    Value="Center" />
            <!--<Setter Property="Margin"
                  Value="20,2,0,2" />-->
            <Setter Property="Foreground"
                    Value="Black" />
            <!--<Setter Property="Background"
                    Value="Transparent" />-->
            <Setter Property="BorderBrush"
                    Value="Transparent" />
            <Setter Property="BorderThickness"
                    Value="2" />
            <Setter Property="CaretBrush"
                    Value="DimGray" />
            <Style.Triggers>
                <Trigger Property="Validation.HasError"
                         Value="true">
                    <Setter Property="ToolTip"
                            Value="{Binding RelativeSource={x:Static RelativeSource.Self},Path=(Validation.Errors)[0].ErrorContent}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="2,2,2,2">
        <Border Margin="0"
                Background="WhiteSmoke"
                BorderThickness="1,1,1,1"
                CornerRadius="2">
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0"
                                     EndPoint="1,1"
                                     MappingMode="RelativeToBoundingBox"
                                     SpreadMethod="Pad">
                    <LinearGradientBrush.GradientStops>
                        <GradientStop Color="Black"
                                      Offset="0.0" />
                        <GradientStop Color="WhiteSmoke"
                                      Offset="0.5" />
                        <GradientStop Color="Black"
                                      Offset="1.0" />
                    </LinearGradientBrush.GradientStops>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <Grid Visibility="{Binding Path=IsLoading,Converter={StaticResource booleanNegationToVisibilityConverter}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="120" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="130" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="70" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="1"
                      Margin="50,0,60,0"
                      FocusManager.FocusedElement="{Binding ElementName=UserNameTextBox}">
                    <Grid.RowDefinitions>
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Vertical">
                        <Label Content="输入用户名:"
                               FontSize="22"
                               FontWeight="Bold"
                               VerticalContentAlignment="Center"
                               HorizontalContentAlignment="Center"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Left"
                               Margin="0,0,0,10"></Label>
             
                    <TextBox x:Name="UserNameTextBox"
                             Text="{Binding Path=UserName,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,ValidatesOnDataErrors=True}"
                             Style="{StaticResource UserNameTextBoxStyle}"
                             Validation.ErrorTemplate="{StaticResource UserNameRrrorTemplate}"
                             FocusManager.IsFocusScope="True"
                             Panel.ZIndex="2">
                        <ei:Interaction.Triggers>
                            <ei:EventTrigger EventName="Loaded">
                                <behaviors:SetFocusAction Delay="10"
                                                          UseKeyboardFocus="True"
                                                          SelectAll="True" />
                            </ei:EventTrigger>
                        </ei:Interaction.Triggers>
                        </TextBox>
                    </StackPanel>
                </Grid>
                
                <Button Grid.Row="3"
                        Margin="0,0,0,0"
                        Width="100"
                        Height="50"
                        Content="{materialDesign:PackIcon Kind=CheckOutline,Size=50}"
                        Command="{Binding Path=ResetLockoutCommand}"
                        Background="{Binding Path=(Border.Background),RelativeSource={RelativeSource Mode=FindAncestor,AncestorType={x:Type Border}}}"
                        FontSize="18"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        ToolTip="Login" />

                <TextBlock Grid.Row="5"
                           Text="{Binding Path=ResponseMessage,Mode=OneWay}"
                          FontSize="22"
                           TextDecorations="Underline"
                           TextWrapping="Wrap"
                           HorizontalAlignment="Left" Background="AntiqueWhite"
                           VerticalAlignment="Center"
                           Margin="50,0,30,0">
                </TextBlock>
            </Grid>
        </Border>

        <Grid Visibility="{Binding Path=IsLoading,Converter={StaticResource booleanToVisibilityConverter}}"
              Margin="10,10,10,10">
            <Label Name="statusLabel"
                   Content="{Binding Path=StatusMessage,Mode=OneWay}"
                   Width="320"
                   Height="30"
                   FontSize="16"
                   FontWeight="Bold"
                   VerticalContentAlignment="Center"
                   HorizontalContentAlignment="Center"
                   Background="Green"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,30">
            </Label>
        </Grid>
    </Grid>
</UserControl>
