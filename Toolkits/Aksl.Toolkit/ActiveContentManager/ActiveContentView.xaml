<UserControl x:Class="Aksl.ActiveContentManager.Views.ActiveContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ei="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:views="clr-namespace:Aksl.ActiveContentManager.Views"
             xmlns:viewmodels="clr-namespace:Aksl.ActiveContentManager.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <UserControl.Resources>
        
        <Style x:Key="dragDropItemContainerStyle"
               TargetType="{x:Type ContentPresenter}">
            <Setter Property="HorizontalAlignment"
                    Value="Stretch" />
            <Setter Property="VerticalAlignment"
                    Value="Stretch" />
        </Style>
        
        <ItemsPanelTemplate x:Key="activeContentItemsPanelTemplate">
            <Grid></Grid>
        </ItemsPanelTemplate>

        <DataTemplate x:Key="activeContentDataTemplate"
                      DataType="{x:Type viewmodels:ActiveContentItemViewModel}">
            <views:ActiveContentItemView />
        </DataTemplate>

        <Style x:Key="activeContentListViewStyle"
               TargetType="{x:Type ListView}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListView}">
                        <Border CornerRadius="2"
                                BorderThickness="2">
                            <ScrollViewer VerticalScrollBarVisibility="Disabled"
                                          HorizontalScrollBarVisibility="Auto">
                                <ItemsPresenter />
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="activeContentListViewItemStyle"
               TargetType="{x:Type ListViewItem}">
            <Setter Property="Cursor"
                    Value="Hand" />
            <Setter Property="IsSelected"
                    Value="{Binding IsSelected,Mode=TwoWay}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListViewItem}">
                        <Border Name="BD_Collector"
                                BorderThickness="1"
                                BorderBrush="Black"
                                CornerRadius="2"
                                Margin="0,0,5,0">
                            <ContentPresenter x:Name="ContentPresenter"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              Content="{TemplateBinding Content}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter TargetName="BD_Collector"
                                        Property="Background"
                                        Value="#FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsSelected"
                                     Value="True">
                                <Setter TargetName="BD_Collector"
                                        Property="Background"
                                        Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ItemsControl ItemsSource="{Binding Path=ActiveContentItems}"  
                  ItemsPanel="{Binding Source={StaticResource activeContentItemsPanelTemplate}}"
                  ItemTemplate="{Binding Source={StaticResource activeContentDataTemplate}}"> 
    </ItemsControl>

    <!--<ListView ItemsSource="{Binding Path=ActiveContentItems}"
              Style="{StaticResource activeContentListViewStyle}"
              ItemsPanel="{StaticResource activeContentItemsPanelTemplate}"
              ItemContainerStyle="{StaticResource activeContentListViewItemStyle}"
              ItemTemplate="{StaticResource activeContentDataTemplate}"
              SelectionMode="Single"
              SelectedItem="{Binding Path=SelectedContentItem,Mode=TwoWay}">
    </ListView>-->
    
</UserControl>
