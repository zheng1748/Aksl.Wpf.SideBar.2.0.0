<UserControl x:Class="Aksl.Toolkit.Dialogs.ConfirmView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:converters="clr-namespace:Aksl.Toolkit.Converters"
             mc:Ignorable="d"
             Width="800"
             Height="400"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="booleanToVisibilityConverter" />
        <converters:BooleanNegationToVisibilityConverter x:Key="booleanNegationToVisibilityConverter" />
    </UserControl.Resources>
    <Grid x:Name="LayoutRoot"
          Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="5" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0"
                Margin="0"
                Background="WhiteSmoke"
                BorderThickness="2"
                CornerRadius="5"
                BorderBrush="Black">
            <TextBlock Margin="7"
                       Text="{Binding Message}"
                       TextWrapping="Wrap"
                       FontSize="16"
                       Foreground="Black" />
        </Border>
        <StackPanel Orientation="Horizontal"
                    Grid.Row="2"
                    HorizontalAlignment="Center">
            <Button x:Name="OkButton"
                    Content="{Binding OkText}"
                    Command="{Binding OKCommand}"
                    CommandParameter="true"
                    Width="90"
                    Height="30"
                    HorizontalAlignment="Right"
                    Margin="0,4,0,8">
            </Button>
            <Button x:Name="CancelButton"
                    Visibility="{Binding Path=IsConfirm,Mode=OneWay,Converter={StaticResource booleanToVisibilityConverter}}"
                    Content="{Binding CancelText}"
                    Command="{Binding CancelCommand}"
                    CommandParameter="false"
                    Width="90"
                    Height="30"
                    HorizontalAlignment="Right"
                    Margin="10,4,0,8">
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
